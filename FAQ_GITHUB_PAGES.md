# ❓ GitHub Pages - Частые вопросы

## Общие вопросы

### Q: Какой URL будет у моего сайта?

**A:** `https://ваш-username.github.io/latechnique/`

Например, если ваш username `johndoe`, то:
- URL: https://johndoe.github.io/latechnique/

### Q: Могу ли я использовать свой домен?

**A:** Да! В Settings → Pages → Custom domain введите ваш домен.

Затем настройте DNS:
```
Type: CNAME
Name: www
Value: ваш-username.github.io
```

### Q: Сколько стоит GitHub Pages?

**A:** **Бесплатно!** Для публичных репозиториев без ограничений.

### Q: Есть ли ограничения?

**A:** Да, некоторые:
- Размер сайта: до 1 GB
- Bandwidth: 100 GB/месяц (мягкий лимит)
- Builds: 10 builds/час
- Только статические сайты (нет серверного кода)

Для текущего проекта это более чем достаточно!

---

## Технические вопросы

### Q: Почему я вижу 404 на всех страницах кроме главной?

**A:** Проверьте `basePath` в `next.config.js`:

```js
// Должно совпадать с названием вашего репо!
basePath: '/latechnique'  // ← измените если репо называется иначе
```

### Q: Стили не загружаются, всё сломано

**A:** Добавьте файл `public/.nojekyll`:

```bash
touch public/.nojekyll
git add public/.nojekyll
git commit -m "fix: add .nojekyll for GitHub Pages"
git push
```

Этот файл говорит GitHub Pages не игнорировать файлы с подчеркиванием.

### Q: Workflow не запускается автоматически

**A:** Проверьте permissions:

1. Settings → Actions → General
2. Workflow permissions → "Read and write permissions"
3. Save
4. Push снова

### Q: Build failed с ошибкой

**A:** Проверьте локально:

```bash
# Соберите проект локально
npm run build

# Если есть ошибки TypeScript/ESLint
npm run lint

# Исправьте ошибки, затем:
git add .
git commit -m "fix: resolve build errors"
git push
```

### Q: Изменения не появляются на сайте

**A:** Несколько причин:

1. **Кэш браузера:** Hard refresh (`Ctrl+Shift+R`)
2. **Build не завершился:** Проверьте Actions
3. **DNS кэш** (если используете свой домен): Подождите до 24 часов

### Q: Как проверить статус деплоя?

**A:** Repository → Actions → Deploy to GitHub Pages

Статусы:
- 🟡 В процессе (1-2 минуты)
- ✅ Успешно
- ❌ Ошибка (кликните чтобы увидеть логи)

---

## Next.js специфичные вопросы

### Q: Почему я не могу использовать API routes?

**A:** GitHub Pages поддерживает только статические сайты.

Для **Этапа 1** (landing page) это не проблема!

Для **Этапов 4-6** (API, worker, payment) нужно будет мигрировать на:
- **Vercel** (рекомендуется для Next.js)
- **Netlify**
- **Railway**

### Q: Image Optimization не работает

**A:** Это нормально! В `next.config.js` уже настроено:

```js
images: {
  unoptimized: true  // GitHub Pages не поддерживает это
}
```

### Q: Можно ли использовать динамические роуты?

**A:** Да, но с ограничениями.

Статические динамические роуты (с `generateStaticParams`) работают.
Полностью динамические роуты требуют серверного рендеринга.

Для landing page это не проблема.

### Q: SSR/ISR работает?

**A:** Нет, GitHub Pages = статический хостинг.

- SSR (Server Side Rendering) ❌
- ISR (Incremental Static Regeneration) ❌
- SSG (Static Site Generation) ✅

Текущий проект использует SSG — всё работает!

---

## Вопросы по процессу деплоя

### Q: Как часто обновляется сайт?

**A:** При каждом `git push` в ветку `main`.

```bash
git add .
git commit -m "update"
git push
# → Автоматический деплой (1-2 мин)
```

### Q: Можно ли деплоить из другой ветки?

**A:** Да! В `.github/workflows/deploy.yml` измените:

```yaml
on:
  push:
    branches: [main, develop]  # ← добавьте нужные ветки
```

### Q: Как сделать preview для Pull Request?

**A:** Создайте отдельный workflow `.github/workflows/preview.yml`:

```yaml
name: PR Preview
on:
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm ci
      - run: npm run build
```

Это только проверит, что билд проходит. Полноценный preview требует сторонних сервисов.

### Q: Сколько времени занимает деплой?

**A:** Обычно 1-2 минуты:
- Build: 30-60 секунд
- Deploy: 20-40 секунд
- Propagation: 10-30 секунд

### Q: Можно ли откатить деплой?

**A:** Да, через Git:

```bash
# Найдите хороший коммит
git log --oneline

# Откатитесь
git revert <commit-hash>
git push

# Или hard reset (опасно!)
git reset --hard <commit-hash>
git push --force
```

---

## Производительность

### Q: Как ускорить время сборки?

**A:** В workflow уже настроено кэширование:

```yaml
- uses: actions/setup-node@v4
  with:
    cache: 'npm'  # ← Кэширует dependencies
```

Первый build: ~2 минуты  
Последующие: ~1 минута

### Q: Как оптимизировать размер бандла?

**A:** 

```bash
# Анализ бандла
npm install --save-dev @next/bundle-analyzer

# В next.config.js добавьте:
const withBundleAnalyzer = require('@next/bundle-analyzer')({
  enabled: process.env.ANALYZE === 'true',
})

module.exports = withBundleAnalyzer(nextConfig)

# Запуск анализа
ANALYZE=true npm run build
```

### Q: Включен ли compression?

**A:** GitHub Pages автоматически сжимает файлы (gzip/brotli).

---

## Миграция и апгрейд

### Q: Когда стоит мигрировать с GitHub Pages?

**A:** Когда нужно:
- API routes (серверный код)
- SSR/ISR
- Больше 100 GB bandwidth/месяц
- Приватный репозиторий с Pages

Для **Этапа 1-3** GitHub Pages идеален ✅  
Для **Этапа 4+** рекомендуется Vercel

### Q: Как мигрировать на Vercel?

**A:** Очень просто:

1. Удалите из `next.config.js`:
```js
// Удалите эти строки
output: 'export',
basePath: '/latechnique',
assetPrefix: '/latechnique/',
```

2. Push на GitHub

3. Подключите к Vercel:
```bash
npm i -g vercel
vercel
```

4. Готово! Vercel автоматически деплоит при каждом push.

### Q: Сохранится ли GitHub Pages URL?

**A:** Да, оба будут работать:
- GitHub Pages: `username.github.io/latechnique`
- Vercel: `latechnique.vercel.app`

Можете использовать GitHub Pages для staging, Vercel для production.

---

## Безопасность и конфиденциальность

### Q: Безопасно ли хранить код на GitHub?

**A:** Для публичного сайта — да!

**НЕ** храните в коде:
- ❌ API keys
- ❌ Secrets
- ❌ Пароли
- ❌ Private tokens

Используйте:
- ✅ Environment variables (GitHub Secrets)
- ✅ `.env.local` (в `.gitignore`)

### Q: Видны ли мои переменные окружения?

**A:** Нет, если используете GitHub Secrets:

```
Settings → Secrets and variables → Actions → New secret
```

В workflow:
```yaml
env:
  API_KEY: ${{ secrets.API_KEY }}
```

### Q: Можно ли скрыть исходный код?

**A:** Нет, для GitHub Pages бесплатно работает только с публичными репо.

Для приватных репо:
- GitHub Pages требует GitHub Pro ($4/мес)
- Или используйте другой хостинг (Vercel, Netlify)

---

## Поддержка и помощь

### Q: Где получить помощь?

**A:** 

1. **Документация:**
   - [GITHUB_PAGES.md](./GITHUB_PAGES.md) — полная инструкция
   - [QUICK_DEPLOY.md](./QUICK_DEPLOY.md) — быстрый старт

2. **GitHub:**
   - [GitHub Pages Docs](https://docs.github.com/en/pages)
   - [Next.js Static Export](https://nextjs.org/docs/app/building-your-application/deploying/static-exports)

3. **Создайте Issue:**
   - [GitHub Issues](https://github.com/yourusername/latechnique/issues)

4. **Community:**
   - [Next.js Discord](https://nextjs.org/discord)
   - [GitHub Community](https://github.com/community)

### Q: Как сообщить о баге?

**A:** Используйте Issue template:

```
Repository → Issues → New issue → Bug Report
```

---

## Полезные команды

```bash
# Проверить статус Git
git status

# Посмотреть последние коммиты
git log --oneline -10

# Локальная сборка (как на GitHub)
npm run build

# Локальный просмотр статического сайта
npm run serve

# Очистить всё и пересобрать
rm -rf .next out node_modules
npm install
npm run build

# Проверить TypeScript
npx tsc --noEmit

# Проверить линтинг
npm run lint
```

---

**Не нашли ответ?** Создайте [Issue](https://github.com/yourusername/latechnique/issues) или напишите на support@latechnique.com
