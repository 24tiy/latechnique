# 🌐 Деплой на GitHub Pages - Пошаговая инструкция

## ✅ Что уже настроено

В проекте уже настроено всё необходимое:
- ✅ `next.config.js` — конфигурация для статического экспорта
- ✅ `.github/workflows/deploy.yml` — автоматический деплой
- ✅ `public/.nojekyll` — для корректной работы на GitHub Pages

---

## 🚀 Шаг 1: Загрузите код на GitHub

### Если репозиторий еще не создан:

```bash
# В папке latechnique
git init
git add .
git commit -m "feat: initial commit with design system and landing page"

# Создайте репозиторий на GitHub (https://github.com/new)
# Назовите его: latechnique

git remote add origin https://github.com/ваш-username/latechnique.git
git branch -M main
git push -u origin main
```

### Если файлы уже на GitHub:

```bash
# Просто обновите файлы
git add .
git commit -m "feat: configure GitHub Pages deployment"
git push origin main
```

---

## ⚙️ Шаг 2: Настройте GitHub Pages

### 1. Откройте Settings репозитория

Перейдите: `https://github.com/ваш-username/latechnique/settings`

### 2. Перейдите в секцию Pages

Боковое меню → **Pages**

### 3. Настройте Source

**Build and deployment:**
- Source: `GitHub Actions` (выберите из dropdown)

**НЕ выбирайте** "Deploy from a branch" — используйте именно GitHub Actions!

### 4. Сохраните настройки

GitHub автоматически применит изменения.

---

## 🎯 Шаг 3: Запустите деплой

### Автоматический деплой

После настройки, каждый `git push` в ветку `main` автоматически:
1. Соберет проект (`npm run build`)
2. Создаст статические файлы в папке `out/`
3. Задеплоит на GitHub Pages

### Ручной деплой

Если хотите задеплоить прямо сейчас:

1. Перейдите в **Actions** вашего репозитория
2. Выберите **Deploy to GitHub Pages** workflow
3. Нажмите **Run workflow** → **Run workflow**
4. Дождитесь завершения (обычно 1-2 минуты)

---

## 🌍 Шаг 4: Откройте сайт

### URL вашего сайта:

```
https://ваш-username.github.io/latechnique/
```

**Пример:**
- Username: `johndoe`
- URL: https://johndoe.github.io/latechnique/

### Проверка деплоя

1. **Actions** → выберите последний workflow
2. Убедитесь, что оба job'а (`build` и `deploy`) прошли успешно ✅
3. Откройте URL сайта

---

## 🔧 Важные настройки

### BasePath в next.config.js

```js
basePath: process.env.NODE_ENV === 'production' ? '/latechnique' : ''
```

**⚠️ ВАЖНО:** Если название вашего репозитория **НЕ** `latechnique`, измените это:

```js
basePath: process.env.NODE_ENV === 'production' ? '/ваше-название-репо' : ''
assetPrefix: process.env.NODE_ENV === 'production' ? '/ваше-название-репо/' : ''
```

### Кастомный домен (опционально)

Если хотите использовать свой домен (например, `latechnique.com`):

1. **Settings** → **Pages** → **Custom domain**
2. Введите домен: `latechnique.com`
3. Настройте DNS:

```
Type: CNAME
Name: www (или @)
Value: ваш-username.github.io
```

4. Дождитесь проверки DNS (может занять до 24 часов)
5. Включите **Enforce HTTPS**

Затем в `next.config.js` уберите basePath:

```js
basePath: process.env.CUSTOM_DOMAIN ? '' : '/latechnique',
assetPrefix: process.env.CUSTOM_DOMAIN ? '' : '/latechnique/',
```

---

## 🐛 Troubleshooting

### Проблема 1: 404 ошибка на сайте

**Причина:** Неправильный basePath в `next.config.js`

**Решение:**
```js
// Проверьте, что название совпадает с именем репо
basePath: '/название-вашего-репо'
```

### Проблема 2: Стили не загружаются

**Причина:** GitHub Pages не видит файлы с `_`

**Решение:** Убедитесь, что файл `public/.nojekyll` существует

```bash
# Проверить
ls -la public/

# Создать, если нет
touch public/.nojekyll
git add public/.nojekyll
git commit -m "fix: add .nojekyll for GitHub Pages"
git push
```

### Проблема 3: Workflow не запускается

**Причина:** Не настроены permissions

**Решение:** 
1. **Settings** → **Actions** → **General**
2. **Workflow permissions** → выберите "Read and write permissions"
3. Сохраните
4. Запустите workflow заново

### Проблема 4: Build failed

**Причина:** Ошибки в коде или зависимостях

**Решение:**
```bash
# Проверьте локально
npm run build

# Если есть ошибки, исправьте их
# Затем:
git add .
git commit -m "fix: resolve build errors"
git push
```

### Проблема 5: Сайт не обновляется

**Причина:** Кэш браузера

**Решение:**
- Hard refresh: `Ctrl+Shift+R` (Windows/Linux) или `Cmd+Shift+R` (Mac)
- Или откройте в режиме инкогнито

---

## 📊 Проверка статуса деплоя

### Через Actions

```
Repository → Actions → Deploy to GitHub Pages
```

Вы увидите:
- 🟡 В процессе (желтый кружок)
- ✅ Успешно (зеленая галочка)
- ❌ Ошибка (красный крестик)

### Через командную строку

```bash
# Посмотреть последние коммиты и статусы
git log --oneline -5

# Посмотреть удаленные ветки
git remote -v

# Проверить, что всё запушено
git status
```

---

## 🔄 Workflow процесс

Вот что происходит при каждом `git push`:

```
1. Push в main
   ↓
2. GitHub Actions запускается автоматически
   ↓
3. Job: Build
   - Checkout кода
   - Установка Node.js 18
   - npm ci (установка зависимостей)
   - npm run build (сборка проекта)
   - Загрузка артефактов (папка out/)
   ↓
4. Job: Deploy
   - Деплой артефактов на GitHub Pages
   ↓
5. Сайт обновлен!
   https://ваш-username.github.io/latechnique/
```

---

## 📈 Мониторинг

### GitHub Actions Badge

Добавьте в README.md:

```markdown
![Deploy](https://github.com/ваш-username/latechnique/actions/workflows/deploy.yml/badge.svg)
```

Это покажет статус последнего деплоя: ![Deploy](https://img.shields.io/badge/deploy-passing-brightgreen)

### Analytics

Для отслеживания посещений установите:
- **Google Analytics** (добавьте в `app/layout.tsx`)
- **Vercel Analytics** (если мигрируете на Vercel)
- **Plausible** (privacy-friendly альтернатива)

---

## ⚡ Оптимизация

### 1. Минимизация времени сборки

В `.github/workflows/deploy.yml` уже настроено кэширование:
```yaml
- uses: actions/setup-node@v4
  with:
    cache: 'npm'  # ← Кэширует node_modules
```

### 2. Deploy preview для PR

Создайте `.github/workflows/preview.yml`:

```yaml
name: Deploy Preview
on:
  pull_request:
    branches: [main]

jobs:
  preview:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '18'
      - run: npm ci
      - run: npm run build
      - name: Comment PR
        uses: actions/github-script@v7
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ Build succeeded! Deploy when merged.'
            })
```

---

## 🎉 Готово!

Теперь ваш сайт автоматически деплоится на GitHub Pages при каждом пуше в `main`!

**Ваш сайт:** `https://ваш-username.github.io/latechnique/`

### Следующие шаги:

1. ✅ Проверьте сайт на всех устройствах (mobile/tablet/desktop)
2. ✅ Настройте кастомный домен (опционально)
3. ✅ Добавьте Google Analytics (опционально)
4. ✅ Поделитесь ссылкой! 🚀

---

## 💡 Альтернативы GitHub Pages

Если нужны серверные функции (API routes, SSR), рассмотрите:

- **Vercel** — рекомендуется для Next.js (бесплатный план)
- **Netlify** — хороший функционал, бесплатный план
- **Railway** — для full-stack приложений
- **Cloudflare Pages** — быстрый и бесплатный

Для текущего **Этапа 1** (только landing page) **GitHub Pages идеально подходит!** ✅

---

**Успешного деплоя! 🚀**
