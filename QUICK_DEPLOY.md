# ⚡ GitHub Pages - Быстрый деплой

## 🎯 TL;DR (3 минуты)

Файлы уже на GitHub? Просто сделайте это:

### 1. Настройте Pages (30 секунд)

```
Ваш репозиторий → Settings → Pages
↓
Source: выберите "GitHub Actions"
↓
Save
```

### 2. Запустите деплой (10 секунд)

```
Ваш репозиторий → Actions → Deploy to GitHub Pages
↓
Run workflow → Run workflow
↓
Ждите 1-2 минуты
```

### 3. Откройте сайт (5 секунд)

```
https://ваш-username.github.io/latechnique/
```

**Готово!** 🎉

---

## 🔧 Важно проверить

### В `next.config.js` должно быть:

```js
basePath: '/latechnique'  // ← Название вашего репо
```

**Если ваш репо называется по-другому**, измените на:

```js
basePath: '/ваше-название-репо'
assetPrefix: '/ваше-название-репо/'
```

---

## ❌ Если что-то не работает

### Сайт показывает 404

```bash
# Проверьте название репо в next.config.js
# Должно совпадать!
```

### Workflow не запускается

```
Settings → Actions → General
→ Workflow permissions
→ Read and write permissions
→ Save
```

### Стили не загружаются

```bash
# Проверьте, что есть файл
ls public/.nojekyll

# Если нет, создайте:
touch public/.nojekyll
git add public/.nojekyll
git commit -m "fix: add .nojekyll"
git push
```

---

## 📚 Полная инструкция

Если нужны детали → читайте **[GITHUB_PAGES.md](./GITHUB_PAGES.md)**

---

## 🚀 Автоматический деплой

После настройки, каждый раз когда вы делаете:

```bash
git add .
git commit -m "feat: обновил что-то"
git push
```

**Сайт автоматически обновляется!** ✨

Проверить статус: `Repository → Actions`

---

**Готово! Сайт живой на GitHub Pages! 🎉**
